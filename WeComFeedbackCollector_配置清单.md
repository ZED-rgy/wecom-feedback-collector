# 企微客户群反馈助手｜使用与配置清单

这份清单和 `WeComFeedbackCollector.exe` 一起交付。每台电脑使用各自登录的企微账号、目标群和智能表格权限，不要直接复制开发机配置。

## 一、交付文件

- [ ] `WeComFeedbackCollector.exe`
- [ ] 本文件 `WeComFeedbackCollector_配置清单.md`

不要发送以下内容：

- 开发机根目录的 `.env`；
- `data`、`logs` 文件夹或其中的数据库；
- 任何 Bot Secret、Token、私钥或企微登录信息。

## 二、运行前提

- [ ] Windows 10/11 64 位电脑；
- [ ] 已安装企业微信桌面端，并登录准备收发消息的员工账号；
- [ ] 电脑允许长期运行，自动发送期间 Windows 必须保持“已登录且未锁屏”；
- [ ] 已安装 Microsoft Edge WebView2 Runtime（大多数 Windows 电脑已自带）；
- [ ] 电脑可以访问企微智能表格和 `wecom-cli` 服务。

说明：接收消息可以在后台运行，企微群窗口不需要一直显示；自动发送时程序会短暂切换到企微窗口，因此不能锁屏或退出企微。

## 三、智能表格一次性授权（需要完整链路时）

在 PowerShell 中执行：

```powershell
npm install -g @wecom/cli
wecom-cli auth init
wecom-cli auth show
```

授权时按页面提示完成登录。`auth show` 显示已授权后，再打开本程序配置页面。若只测试本地收消息，可暂不配置这一步，但智能表格读取/写入会不可用。

## 四、首次配置

1. 双击启动 `WeComFeedbackCollector.exe`，打开本机配置页面（通常是 `http://127.0.0.1:8765`）。
2. 在“设置”中填写并保存：

   - 当前目标群：填写要监控的群名称；
   - 群备注：有备注时一并填写，用于发送前校验；
   - 企微账号名称：填写本机已登录、会被群成员 `@` 的员工显示名；
   - 智能表格链接：粘贴目标智能表格地址；
   - 表格机器人 ID、Secret：填写该机器人自己的凭据；
   - 摘要间隔：按需要设置，默认每 2 小时；
   - 摘要模板：按群成员阅读习惯调整；
   - `dry-run`：首次测试保持开启；
   - 自动发送：首次测试保持关闭。

3. 点击“连接检测/重新检测”，确认企微消息读取、智能表格和摘要调度均正常。
4. 在目标群发送一条明确 `@企微账号 + 问题描述` 的测试消息，确认反馈中心出现记录，且智能表格新增一行。
5. 确认无误后，关闭 `dry-run` 并开启“自动发送”，再手动发送一次测试摘要。

## 五、日常运行

- 程序启动后会驻留系统托盘并持续轮询；关闭配置窗口只会隐藏到托盘，不会停止后台服务。
- 同时只监听一个客户群。换群时在“设置”中点击“更换监听群”，验证成功后再保存。
- 只有 `@` 目标企微账号的消息会进入反馈整理流程；普通聊天不会入库。
- 摘要默认统计“自上次摘要后新增”，并按智能表格中的任务状态汇总。
- 自动发送依赖企微桌面端处于登录状态，Windows 锁屏、退出登录或远程会话不可交互时可能无法发送。

## 六、常见问题

### 智能表格显示无法读取

先在 PowerShell 执行：

```powershell
wecom-cli auth show
wecom-cli auth init
```

然后回到程序设置页重新检测。若智能表格文档权限或授权已过期，需要由有权限的账号重新授权；“已配置”只表示已填写凭据，连接检测结果才表示当前是否可访问。

### 新的 @消息没有记录

- 确认消息发在当前配置的目标群，而不是同名的其他群；
- 确认确实 `@` 了配置中的企微账号；
- 确认企微仍登录，程序托盘状态为运行中；
- 点击工作台/反馈中心的刷新，等待一个轮询周期（默认约 10 秒）。

### 摘要发送失败

- Windows 保持登录、未锁屏；
- 企业微信保持登录并可正常搜索目标群；
- 群名/备注不要使用容易重复的名称；
- 在“运行记录”查看失败原因，修正后使用“人工确认后重试”。

## 七、配置和数据位置

程序会把当前用户的配置、数据库和日志放在：

```text
%LOCALAPPDATA%\WeComFeedbackCollector\
```

升级程序时只替换 `WeComFeedbackCollector.exe`，不要删除上述目录。Secret 在 Windows 上优先使用当前用户绑定的 DPAPI 保存，换电脑或换 Windows 用户后需要重新配置。

## 八、交付前验收

- [ ] 能启动程序并打开配置页面；
- [ ] 企微消息读取状态正常；
- [ ] 智能表格连接检测通过；
- [ ] 测试 `@` 消息能写入智能表格；
- [ ] dry-run 测试摘要内容正确；
- [ ] 关闭 dry-run 后能成功发送到目标群；
- [ ] 重启程序后配置仍保留；
- [ ] 电脑锁屏时已知晓自动发送会暂停，解锁后可继续运行。

